import 'package:flutter/material.dart';

class QuoteForm extends StatelessWidget {
  const QuoteForm({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text('Quote Form'),
        ),
      ),
    );
  }
}
